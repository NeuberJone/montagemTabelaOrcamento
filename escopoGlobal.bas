Dim manualStatus As Boolean ' Retorna se a função Manual está ativa ou não
Dim sacolaStatus As Boolean ' Retorna se a função Sacola está ativa ou não
Dim informacaoClienteStatus As Boolean ' Retorna se a função Informação Clientes está ativa ou não
Dim larguraXaltura As Boolean ' Retorna se a função Largura x Altura está ativa ou não
Dim larguraXprofundidadeXaltura As Boolean ' Retorna se a função Largura x Profundidade x Altura está ativa ou não
Dim tamanhoProfundidade As Variant ' Armazena o valor do tamanho profundidade
